```
git clone https://github.com/lalilisa/text-to-speech.git
cd vntts
```

```
tắt cổng 80 (nếu có) trên docker để tránh conflict trc khi run cmd bên dưới
docker compose build
docker compose up -d
```

```
api-docs: http://localhost:80/docs

interface gradio: http://localhost:80/gradio
```
```
```